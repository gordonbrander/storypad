doctype 5
- var classes = classname(document.classes, 'slide slide-fullscreen')
- var htmlStyle = [document.html_style, document.slide_style].join(' ')
html(style=htmlStyle, class=classes)
  head
    include head
    include next-link
  body
    block content
      != content

    include artboard-nav
    include foot
